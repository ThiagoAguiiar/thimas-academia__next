export interface IPostFormData {
  email: string;
  name: string;
  phoneNumber: string;
  subject: string;
}

export interface IGetFormData {
  saqId: string;
  createdAt: string;
  email: string;
  name: string;
  phoneNumber: string;
  subject: string;
}
