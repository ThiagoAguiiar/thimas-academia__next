export interface IPostFormData {
  email: string;
  name: string;
  phoneNumber: string;
  subject: string;
  isActive: boolean;
}

export interface IGetFormData {
  saqId: string;
  createdAt: string;
  email: string;
  name: string;
  phoneNumber: string;
  subject: string;
  isActive: boolean;
}

export interface IPutFormData {
  saqId: string;
  createdAt: string;
  email: string;
  name: string;
  phoneNumber: string;
  subject: string;
  isActive: boolean;
}
